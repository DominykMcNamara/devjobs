import jobsData from '../../../../data.json'

const handler = async ({ query: {title, location, fullTime}}, res) => {
    try {

    } catch (err) {
        console.log(err)
    }
}

export default handler